dependencies:
    pre:
        - npm install -g npm

machine:
    node:
        version: 4.3.0

deployment:
    staging:
        branch: master
        heroku:
            appname: tictactoe-jonathanchrisp
